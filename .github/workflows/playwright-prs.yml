name: Playwright Tests PR
on:
   pull_request:

jobs:
  test:
    name: Integration tests
    uses: ./.github/workflows/playwright.yml
    secrets: inherit
    with:
      environment: test
      url: 'https://arns-assessment-platform-test.hmpps.service.justice.gov.uk'
